apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ayatsuri-data
  namespace: ayatsuri-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # For local k8s (Docker Desktop, minikube, kind), the default storageClass works
  # storageClassName: standard
