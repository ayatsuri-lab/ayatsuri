apiVersion: apps/v1
kind: Deployment
metadata:
  name: ayatsuri-server
  namespace: ayatsuri-dev
  labels:
    app: ayatsuri
    component: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ayatsuri
      component: server
  template:
    metadata:
      labels:
        app: ayatsuri
        component: server
    spec:
      containers:
      - name: ayatsuri
        # Use official latest image
        image: ghcr.io/ayatsuri-lab/ayatsuri:latest
        imagePullPolicy: IfNotPresent
        command: ["ayatsuri", "start-all"]
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 50055
          name: grpc
          protocol: TCP
        envFrom:
        - configMapRef:
            name: ayatsuri-config
        env:
        # Enable distributed mode: coordinator binds to all interfaces and advertises service name
        # Note: start-all only starts coordinator when host is not 127.0.0.1/localhost
        - name: AYATSURI_COORDINATOR_HOST
          value: "0.0.0.0"
        - name: AYATSURI_COORDINATOR_ADVERTISE
          value: "ayatsuri-server"
        volumeMounts:
        - name: ayatsuri-data
          mountPath: /var/lib/ayatsuri
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: ayatsuri-data
        persistentVolumeClaim:
          claimName: ayatsuri-data
---
apiVersion: v1
kind: Service
metadata:
  name: ayatsuri-server
  namespace: ayatsuri-dev
  labels:
    app: ayatsuri
    component: server
spec:
  type: NodePort
  selector:
    app: ayatsuri
    component: server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
  - name: grpc
    port: 50055
    targetPort: 50055
    protocol: TCP
