apiVersion: v1
kind: Service
metadata:
  name: {{ include "ayatsuri.fullname" . }}-scheduler
  labels:
    {{- include "ayatsuri.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler
spec:
  type: ClusterIP
  selector:
    {{- include "ayatsuri.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduler
  ports:
    - name: health
      port: 8090
      targetPort: health
      protocol: TCP
